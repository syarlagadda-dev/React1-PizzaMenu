Instructions to boot up webpage:

1. Clone repository onto your local system.
2. Once downloaded, open up your powershell and change directory into the project folder.
3. Run "npm install" to download dependencies.
4. Run "npm start" to boot up the webpage onto the local host.
5. Navigate to http://localhost:3000

You're done!
